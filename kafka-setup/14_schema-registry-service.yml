apiVersion: v1
kind: Service
metadata:
  labels:
    service: schema-registry
  name: schema-registry
  namespace: event-streaming-platform
spec:
  selector:
    service: schema-registry
  ports:
    - port: 30081
      name: outport
      targetPort: 30081
      nodePort: 30081
  type: NodePort